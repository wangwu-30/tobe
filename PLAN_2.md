# 默认轻搜 + 可选深度研究（纳入评论触发的 MVP 方案）

状态：2026-03-17 已完成，相关落地事实已并入 [项目状态](./docs/chengxing-project-status.md)。

## 现状

- 当前联网能力只在 `Chat` 里显式存在，仍是旧的 `searchMode: auto | force` 和“仅草稿 / 实时网页已开启”切换。
- `search_web`、搜索 provider 设置、workspace 文件写入链路已经存在，但没有轻搜预算、没有深研计划确认、没有阶段进度、没有研究报告产出壳。
- 评论线程目前走独立的 `comment-reply` 路由，默认是局部改写型短回复，不带工具调用，也没有研究计划/进度/报告可见性。
- 评论区已经是 `@角色` 驱动、多角色监听窗口模型；这意味着“评论触发深研”不能简单照搬 Chat 的输入区切换，而必须兼顾线程简洁性和报告可追溯性。
- 当前是 MVP 阶段，不做旧逻辑兼容。旧 `searchMode`、旧 Live Web 切换、旧文案、旧测试应一次性删除。

## 目标

把系统收成一条统一语义：`Chat` 默认智能按需轻搜，用户可显式升级为深度研究；`Comment` 仍以简短局部回复为主，但允许显式升级为深度研究，且研究结果始终是“线程里短摘要 + 支持资料里的完整报告”。

## 差距

- 如果只把默认联网做在 Chat，评论一旦需要查外部信息，体验会断裂；但如果把评论普通回复也改成默认联网，又会破坏评论作为“局部修改请求”的轻量定位。
- 如果深研完成后把长报告直接塞进评论线程，线程会失去可读性；但如果只给一句“已研究完”，报告又会变成不可见黑箱。
- 现有 `AssistantRun.payloadJson` 足够承接 Chat 研究计划与进度，但评论线程没有等价的长跑状态壳，需要单独补一层线程级研究状态，而不是硬把评论塞进会话 run 模型。
- Gemini 提的几条建议里，应该吸收的是：轻搜硬预算、预算耗尽时返回“继续离线完成”提示、深研阶段进度枚举、缺口分析的二次调度、报告命名避冲突、彻底清理旧入口；不需要把评论普通回复整体改成默认联网长跑。

## 执行计划

1. 收掉旧联网模式，切成新的研究语义
- 删除 `searchMode: auto | force` 在前端、路由、类型、i18n、E2E 中的全部引用，不做兼容映射。
- `Chat` 请求统一改为 `researchMode: 'light' | 'deep'`，默认 `light`。
- 删除聊天输入区“仅草稿 / 实时网页已开启”切换和相关 banner，只保留一个紧凑的 `深度研究` pill。
- `light` 表示默认联网但仅按需轻搜；用户若明确写“离线处理 / 不要联网”，本轮直接按离线执行，不另加全局开关。

2. 轻搜做成“隐式可用 + 硬预算”
- 在 `createWorkspaceAgentTools` 增加 `researchMode` 与 `searchBudget`，`light` 模式下单次 run 最多 2 次 `search_web`。
- 预算计数器放在单次 `streamWorkspaceAssistantRun` 的工具闭包里，保证同一 assistant run 内多次工具调用共享同一预算。
- 预算耗尽时，`search_web` 不报错、不终止 run，而是返回明确提示给模型：“联网搜索预算已耗尽，请基于现有搜索结果和已有知识继续完成任务。”
- 删除当前 `force` 预抓取搜索分支，不再在路由里先搜再硬塞一段 Web Context。
- `buildChatSystemPrompt` 改成两层：
  - `light`：只核关键事实、最新信息、外部背景
  - `deep`：按研究计划执行、多轮搜索、输出引用报告
- provider 未就绪时：
  - `light`：显示“联网未就绪，本轮按离线处理”，但允许继续
  - `deep`：直接阻断启动并给出设置入口

3. Chat 深研采用“先计划、后执行”的显式升级流
- 新增 `POST /api/ai/research-plan`，专门生成 `researchPlanProposal`，不直接开始长跑。
- Chat 点 `深度研究` 发送后，先出现研究计划卡：
  - 标题
  - 3-5 个子问题
  - 报告提纲
  - 可选限定域名
- 用户确认后，再启动真正的 deep research run。
- 深研执行用共享 `research-runner`：
  - 阶段 1：逐个子问题搜索并汇总
  - 阶段 2：一次轻量 LLM 缺口分析，生成补充查询，再补 1-2 轮搜索，最后整理报告
- Chat 的最终表面不是整篇报告直出，而是：
  - 一段 executive summary
  - 关键结论列表
  - `打开完整报告` CTA

4. 评论线程支持“显式升级为深研”，但默认仍保持简洁
- 评论普通 AI 回复继续保持本地、短平快、局部改写定位；不把默认轻搜直接扩到所有普通评论回复。
- 评论输入区新增与 Chat 同语义的紧凑 `深度研究` pill，但只在可 AI 回复的 direct 线程/创建表单里出现；inherited、只读、无 agent 可达时隐藏或给出原因。
- 评论深研的触发规则：
  - 必须显式开启 `深度研究`
  - 必须有且仅有一个目标 agent
  - 如果本条消息中提及多个 agent，则阻止开启深研并提示“深度研究一次只能交给一个角色”
- 评论开启深研后，不走普通 `comment-reply` 直答，而是：
  - 先生成线程内的 `研究计划卡`
  - 用户确认后再开始研究
  - 线程卡上显示紧凑阶段状态：`研究计划待确认 / 搜索中 / 分析缺口 / 整理报告 / 已完成 / 无法联网`
- 评论深研完成后的线程输出固定为“短摘要而非长报告”：
  - 1 句结论
  - 最多 3 条 bullet
  - 最多 2 条内联引用
  - 一个 `打开完整报告` 动作
- 完整研究报告原文默认不铺在评论线程里，但必须可见：
  - 自动写入支持资料树下的 `研究` 目录
  - 线程里显示报告卡片，点击可直接打开对应支持资料文件
- 评论深研不改变“应用到原文”链路；研究只是产出依据和建议，真正落盘仍走现有评论应用/修改流程。

5. 共享研究产物和状态模型
- 深研报告统一写入支持资料树的 `研究` 目录，按 `<主题> 研究报告` 命名；若重名则自动追加序号，不覆盖旧文件。
- 报告必须包含：
  - 结构化分节
  - 每条关键结论的来源 URL
  - 文末 `参考来源`
  - 不确定项标 `待验证`
- Chat 与 Comment 共用一个 `research-runner`，但状态壳分开：
  - Chat 走 `AssistantRun.payloadJson`
  - Comment 走线程级 `researchState` JSON
- 重要接口与类型：
  - `ChatRequestPayload`：删除 `searchMode`，新增 `researchMode: 'light' | 'deep'`
  - `AssistantRunPayload`：新增 `researchPlanProposal`、`researchProgress`
  - `CommentThreadData`：新增 `researchState`
  - 新增 `DeepResearchPlanProposal`
  - 新增 `POST /api/ai/research-plan`
  - 新增评论侧研究计划/启动接口，例如 `POST /api/threads/[threadId]/research-plan` 与 `POST /api/threads/[threadId]/research/start`
- 深研执行预算固定：
  - 初始子问题最多 5 次搜索
  - 缺口补搜最多 2 次
  - 合计最多 7 次搜索

6. 彻底移除旧入口，并同步门禁
- 清理 `use-chat.ts` 的 FormData、`chat-input.tsx` 的条件渲染、`chat-panel.tsx` 的参数透传、i18n 旧 live web 文案、相关占位文案和样式分支。
- Playwright 统一改写，不再验证旧联网开关，而是验证：
  - 默认轻搜
  - 显式离线不搜索
  - 轻搜预算上限
  - Chat 深研计划确认
  - 评论深研计划确认
  - 评论深研只输出短摘要，完整报告通过支持资料可见
  - provider 缺失阻断
  - 研究报告文件写入 `研究` 目录
- 经验台账补两条结论：
  - 默认能力应隐式可用，升级能力才显式暴露
  - 长跑研究结果不应直接淹没主交互面，线程/聊天只保留摘要，完整报告进入可复用资料层

## 验收标准

- 旧语义清理
  - 代码、类型、文案、测试中不再存在 `searchMode`、`force`、`chat.liveWebOn`、旧“仅草稿 / 实时网页”切换。
  - `use-chat.ts`、`chat-input.tsx`、`chat-panel.tsx` 不再残留旧联网模式逻辑。

- Chat 轻搜与深研
  - 默认发送为 `light`，结构整理类请求不强制搜索。
  - 最新事实类请求会按需触发搜索，且单次 run 不超过 2 次。
  - 预算耗尽时 run 继续，模型收到明确预算耗尽提示。
  - `深度研究` 必须先出研究计划，再执行。
  - 深研完成后会产出完整报告文件，并在聊天里只显示摘要 + 打开报告入口。

- Comment 深研
  - 普通评论回复仍然简洁，不默认变成长跑研究。
  - 评论显式开启深研后，先出研究计划卡，再开始执行。
  - 评论深研一次只能绑定一个 agent。
  - 评论深研完成后，线程里只显示短摘要，不直接灌入整篇报告。
  - 完整报告可从线程卡片打开，并且在支持资料树 `研究` 目录可见。

- 报告与引用
  - 每份研究报告都带来源 URL 和 `参考来源` 段。
  - 重名不会覆盖旧文件。
  - 不确定信息标 `待验证`。

- 门禁
  - 新增/改写的 E2E 覆盖 Chat 与 Comment 两条研究路径。
  - `npm run verify:iteration` 全绿后才算关闭。

## 默认与假设

- MVP 不做旧逻辑兼容，旧联网模式一次性删除。
- `Chat` 默认轻搜；`Comment` 默认仍是本地短回复，深研只在显式升级时触发。
- 评论深研的完整报告默认不 inline 展开；“原文可见”通过打开支持资料文件实现。
- 深研 v1 继续使用仓内 Node/TS 编排，不引入 Python sidecar。
- 开源能力只借鉴 Node/TS deep research 参考项目的 planner / progress / report 结构，不把外部仓库作为运行时依赖。
